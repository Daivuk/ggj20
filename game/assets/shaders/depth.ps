Texture0 texDiffuse;

input float2 inTexCoord;
input float3 inWorldPos;

void main()
{
    float4 diffuse = texDiffuse(inTexCoord);
    if (diffuse.a < 0.8) discard;
    oColor = float4(inWorldPos / 20.0, 1.0);
}
