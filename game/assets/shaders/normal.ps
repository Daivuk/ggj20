Texture0 texDiffuse;

input float3 inNormal;
input float2 inTexCoord;

void main()
{
    float4 diffuse = texDiffuse(inTexCoord);
    if (diffuse.a < 0.8) discard;
    oColor = float4(inNormal.rgb * 0.5 + 0.5, 1.0);
}
